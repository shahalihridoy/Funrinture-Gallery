import { SwipeSliderDirective } from './swipe-slider.directive';

describe('SwipeSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeSliderDirective();
    expect(directive).toBeTruthy();
  });
});
