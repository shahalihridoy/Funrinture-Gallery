import { NgIfDelayDirective } from './ng-if-delay.directive';

describe('NgIfDelayDirective', () => {
  it('should create an instance', () => {
    const directive = new NgIfDelayDirective();
    expect(directive).toBeTruthy();
  });
});
